{
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "test": "lerna exec -- yarn test",
    "test:ci": "lerna exec -- yarn test:ci"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "resolutions": {
    "minimist": "^0.2.1"
  }
}
